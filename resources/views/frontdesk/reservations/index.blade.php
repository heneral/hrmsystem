<x-app-layout>
    <x-slot name="header"><div><p class="text-xs font-semibold uppercase tracking-[0.2em] text-amber-700">Front desk</p><h2 class="mt-1 text-2xl font-semibold text-stone-900">Reservations</h2></div></x-slot>
    <div class="bg-stone-50 py-8"><div class="mx-auto max-w-7xl space-y-6 px-4 sm:px-6 lg:px-8">
        <section class="rounded-xl border border-stone-200 bg-white p-5 shadow-sm"><form method="get" class="grid gap-3 md:grid-cols-6"><input name="search" value="{{ request('search') }}" placeholder="Search confirmation, guest, email, phone, room" class="rounded-lg border-stone-300 text-sm md:col-span-2"><select name="status" class="rounded-lg border-stone-300 text-sm"><option value="">All statuses</option>@foreach(['pending','confirmed','checked_in','checked_out','cancelled','no_show'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ str_replace('_', ' ', ucfirst($status)) }}</option>@endforeach</select><select name="payment_status" class="rounded-lg border-stone-300 text-sm"><option value="">All payments</option>@foreach(['pending','partial','paid','refunded'] as $status)<option value="{{ $status }}" @selected(request('payment_status') === $status)>{{ ucfirst($status) }}</option>@endforeach</select><select name="room_type" class="rounded-lg border-stone-300 text-sm"><option value="">All room types</option>@foreach($roomTypes as $roomType)<option value="{{ $roomType->id }}" @selected((string) request('room_type') === (string) $roomType->id)>{{ $roomType->name }}</option>@endforeach</select><button class="rounded-lg bg-stone-900 px-4 py-2 text-sm font-semibold text-white">Search</button><div class="grid gap-3 sm:grid-cols-2 md:col-span-5"><input type="date" name="from" value="{{ request('from') }}" class="rounded-lg border-stone-300 text-sm"><input type="date" name="to" value="{{ request('to') }}" class="rounded-lg border-stone-300 text-sm"></div><a href="{{ route('frontdesk.reservations.index') }}" class="rounded-lg border border-stone-300 px-4 py-2 text-center text-sm font-semibold text-stone-700">Clear filters</a></form></section>
        <section class="overflow-hidden rounded-xl border border-stone-200 bg-white shadow-sm"><div class="flex items-center justify-between border-b border-stone-200 p-5"><div><h3 class="font-semibold">All reservations</h3><p class="mt-1 text-sm text-stone-500">{{ $reservations->total() }} matching reservations</p></div><a href="{{ route('frontdesk.dashboard') }}" class="text-sm font-semibold text-amber-700">Back to desk</a></div><div class="overflow-x-auto"><table class="w-full min-w-[780px] text-left text-sm"><thead class="bg-stone-50 text-xs uppercase tracking-widest text-stone-500"><tr><th class="px-5 py-3">Reservation</th><th class="px-5 py-3">Guest</th><th class="px-5 py-3">Stay</th><th class="px-5 py-3">Room</th><th class="px-5 py-3">Payment</th><th class="px-5 py-3">Status</th><th></th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($reservations as $reservation)<tr><td class="px-5 py-4 font-semibold">{{ $reservation->reservation_number }}</td><td class="px-5 py-4">{{ $reservation->customer?->name ?? $reservation->guests->first()?->first_name.' '.$reservation->guests->first()?->last_name ?? 'Guest' }}</td><td class="px-5 py-4 text-stone-500">{{ $reservation->check_in->format('M j') }} - {{ $reservation->check_out->format('M j, Y') }}</td><td class="px-5 py-4 text-stone-500">{{ $reservation->room?->room_number ?? 'Unassigned' }}</td><td class="px-5 py-4 uppercase text-stone-500">{{ $reservation->payment_status }}</td><td class="px-5 py-4"><span class="rounded-full bg-stone-100 px-2.5 py-1 text-xs font-semibold uppercase">{{ $reservation->status->value }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('reservations.show', $reservation) }}" class="font-semibold text-amber-700">View</a></td></tr>@empty<tr><td colspan="7" class="px-5 py-10 text-center text-stone-500">No reservations match these filters.</td></tr>@endforelse</tbody></table></div><div class="border-t border-stone-200 p-5">{{ $reservations->links() }}</div></section>
    </div></div>
</x-app-layout>
