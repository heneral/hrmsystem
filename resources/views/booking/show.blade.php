<x-app-layout><x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Reservation {{ $reservation->reservation_number }}</h2></x-slot><div class="py-12"><div class="max-w-4xl mx-auto sm:px-6 lg:px-8"><div class="bg-white overflow-hidden shadow-sm sm:rounded-lg"><div class="p-6 text-gray-900"><p class="text-sm uppercase tracking-widest text-amber-700">{{ $reservation->status->value }}</p><h1 class="mt-2 text-2xl font-semibold">{{ $reservation->room->roomType->name }} · Room {{ $reservation->room->room_number }}</h1><p class="mt-4">{{ $reservation->check_in->format('M d, Y') }} to {{ $reservation->check_out->format('M d, Y') }}</p><p class="mt-2">Total: ${{ number_format($reservation->total, 2) }}</p>@if(in_array($reservation->status->value, ['pending', 'confirmed']))<form method="post" action="{{ route('reservations.cancel', $reservation) }}" class="mt-6">@csrf<button class="rounded bg-red-700 px-4 py-2 text-sm font-semibold text-white">Cancel reservation</button></form>@endif</div></div></div></div></x-app-layout>