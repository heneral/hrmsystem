<x-app-layout>
    <x-slot name="header"><div><p class="text-xs font-semibold uppercase tracking-[0.2em] text-amber-700">Security workspace</p><h2 class="mt-1 text-2xl font-semibold text-stone-900">Audit logs</h2></div></x-slot>
    <div class="min-h-screen bg-stone-50 py-8"><div class="mx-auto max-w-7xl space-y-6 px-4 sm:px-6 lg:px-8">
        <div class="grid gap-4 sm:grid-cols-3">@foreach([['label' => 'Total events', 'value' => $summary['total']], ['label' => 'Events today', 'value' => $summary['today']], ['label' => 'Active actors', 'value' => $summary['actors']] ] as $metric)<div class="rounded-xl border border-stone-200 bg-white p-5 shadow-sm"><p class="text-xs font-semibold uppercase tracking-widest text-stone-500">{{ $metric['label'] }}</p><p class="mt-3 text-3xl font-semibold">{{ $metric['value'] }}</p></div>@endforeach</div>
        <section class="rounded-2xl border border-stone-200 bg-white p-6 shadow-sm"><div><h3 class="text-xl font-semibold">Immutable activity history</h3><p class="mt-1 text-sm text-stone-500">Security and operational actions recorded by HotelHub.</p></div><form method="get" class="mt-5 flex flex-col gap-3 sm:flex-row"><select name="action" class="rounded-lg border-stone-300 sm:max-w-sm"><option value="">All actions</option>@foreach($actions as $action)<option value="{{ $action }}" @selected(request('action') === $action)>{{ $action }}</option>@endforeach</select><button class="rounded-lg bg-stone-900 px-4 py-3 text-sm font-semibold text-white hover:bg-amber-700">Filter activity</button></form></section>
        <section class="overflow-hidden rounded-2xl border border-stone-200 bg-white shadow-sm"><div class="border-b border-stone-200 p-6"><h3 class="text-xl font-semibold">Activity stream</h3><p class="mt-1 text-sm text-stone-500">{{ $logs->total() }} events matching this view</p></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b bg-stone-50 text-xs uppercase tracking-widest text-stone-500"><th class="px-6 py-4">Time</th><th class="px-6 py-4">Action</th><th class="px-6 py-4">Actor</th><th class="px-6 py-4">Entity</th><th class="px-6 py-4">New values</th></tr></thead><tbody>@forelse($logs as $log)<tr class="border-b last:border-0 hover:bg-amber-50"><td class="whitespace-nowrap px-6 py-4 text-stone-600">{{ optional($log->created_at)->format('M d, Y H:i') }}</td><td class="px-6 py-4"><span class="rounded-full bg-amber-50 px-3 py-1 text-xs font-semibold text-amber-800">{{ $log->action }}</span></td><td class="px-6 py-4">{{ $log->user?->email ?? 'System' }}</td><td class="px-6 py-4 font-medium">{{ class_basename($log->entity_type) }} #{{ $log->entity_id }}</td><td class="max-w-xs truncate px-6 py-4 text-xs text-stone-500" title="{{ json_encode($log->new_values) }}">{{ json_encode($log->new_values) }}</td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-stone-500">No audit activity has been recorded yet.</td></tr>@endforelse</tbody></table></div><div class="border-t border-stone-100 p-6">{{ $logs->links() }}</div></section>
    </div></div>
</x-app-layout>
